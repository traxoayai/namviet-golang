// src/shared/constants/defaults.ts

/**
 * ID Kho Mặc Định (Warehouse ID)
 * Sử dụng khi:
 * 1. User hiện tại chưa được map với một kho cụ thể.
 * 2. Màn hình chọn kho bị bỏ sót / không bắt buộc.
 * 
 * Sẽ hữu ích trong giai đoạn hệ thống mới có 1 kho chính để chống lỗi HardCode.
 */
export const DEFAULT_WAREHOUSE_ID = 1;
